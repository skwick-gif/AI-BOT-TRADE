בהתבסס על קריאת הקובץ `start_bridge.bat` שלך (שנמצא ב-root של הפרויקט), אני אסביר את התהליך המדויק שהוא מבצע, שלב אחר שלב. הקובץ הזה הוא סקריפט batch שמנהל את בניית והרצת הגשר (InterReactBridge), והתחברות ל-IBKR. הוא לא מריץ את הקוד הראשי של האפליקציה, אלא רק מכין את הגשר כשירות רקע.

### מבנה כללי של התהליך:
הסקריפט מתחיל בתפריט בחירה למשתמש, בונה את הפרויקט C#, מריץ את הגשר כשרת web, בודק שהוא בריא, ושולח בקשת התחברות ל-IBKR דרך הגשר. הגשר עצמו הוא API REST שרץ על פורט 5080 ומתחבר ל-IBKR.

### השלבים המדויקים (על פי הקוד):
1. **אתחול משתנים**:
   - מגדיר את נתיב הפרויקט: `tools\InterReactBridge\InterReactBridge.csproj` (הפרויקט C# של הגשר).
   - מגדיר את כתובת הגשר: `http://localhost:5080` (הגשר ירוץ כשרת web על הפורט הזה).
   - מגדיר Client ID ברירת מחדל: `101` (לשימוש בחיבור ל-IBKR).

2. **תפריט בחירה למטרת החיבור ל-IBKR**:
   - מציג אפשרויות:
     - 1: IB Gateway (Live) - פורט 4001
     - 2: IB Gateway (Paper) - פורט 4002
     - 3: TWS (Paper) - פורט 7497
     - 4: TWS (Live) - פורט 7496
     - 5: פורט מותאם אישית (מאפשר הזנת פורט ו-Client ID ידנית).
   - על פי הבחירה, מגדיר את הפורט המתאים.
   - אם בוחרים 5, מבקש הזנת פורט ו-Client ID (ברירת מחדל 101 אם לא מוזן).

3. **בניית הפרויקט (BUILD)**:
   - מריץ `dotnet build` על הפרויקט `tools\InterReactBridge\InterReactBridge.csproj` במצב Release.
   - אם הבנייה נכשלת (errorlevel ≠ 0), מדפיס "Build failed" ויוצא.
   - זה בונה את הקוד C# של הגשר (InterReactBridge), שכנראה משתמש בספריית InterReact לחיבור ל-IBKR.

4. **הרצת הגשר (LAUNCH)**:
   - מריץ את הגשר בחלון נפרד (באמצעות `start cmd /k`) עם `dotnet run --project ... --no-build --urls http://localhost:5080`.
   - הגשר רץ כשרת ASP.NET Core על פורט 5080.
   - מחכה 3 שניות כדי שהגשר יתחיל.

5. **בדיקת בריאות הגשר (Verifying Bridge Health)**:
   - שולח בקשת GET ל-`http://localhost:5080/health` באמצעות PowerShell (Invoke-WebRequest).
   - מנסה עד 5 פעמים עם השהיה של 500ms בין ניסיונות.
   - אם מצליח, מדפיס "Bridge is healthy!" עם קוד סטטוס.
   - אם נכשל אחרי 5 ניסיונות, מדפיס שגיאה "Bridge is not responding".

6. **ניסיון התחברות ל-IBKR (Attempting to Connect to IBKR)**:
   - שולח בקשת POST ל-`http://localhost:5080/connect?host=127.0.0.1&port=<PORT>&clientId=<CLIENT_ID>` באמצעות PowerShell (Invoke-RestMethod).
   - זה שולח את הפרטים לגשר כדי שיתחבר ל-IBKR (host=localhost, פורט לפי בחירה, client ID).
   - אם מצליח, מדפיס "Connection attempt sent."
   - אם נכשל, מדפיס את השגיאה.

7. **סיום**:
   - מדפיס הודעה שהגשר רץ ומנסה להתחבר.
   - מציין שניתן להריץ `run_app.bat` בחלון נפרד להרצת ה-UI.
   - ממתין ללחיצה על מקש (pause) לפני סיום.

### קבצים נחוצים שמוזכרים או משומשים:
- **tools\InterReactBridge\InterReactBridge.csproj**: הפרויקט C# לבנייה.
- **tools\InterReactBridge\**: התיקייה עם קוד המקור של הגשר (כולל קבצי .cs).
- **run_app.bat**: סקריפט נפרד להרצת ה-UI (מוזכר בסוף, אבל לא חלק מהתהליך הזה).
- **dotnet CLI**: נדרש להיות מותקן לבנייה והרצה של .NET.

### הערות:
- הגשר הוא שירות REST API שרץ ברקע ומחבר בין הקוד שלך ל-IBKR.
- התהליך לא כולל הרצת האפליקציה הראשית (כמו AI trading), אלא רק הכנת הגשר.
- אם יש בעיות, הן יכולות להיות בבנייה (שגיאות קומפילציה), בהרצה (פורט תפוס), או בחיבור (TWS לא רץ או הגדרות שגויות).
- זה מבוסס רק על הקובץ הזה; אם יש קבצים נוספים שקשורים (כמו קונפיגורציה), הם לא מוזכרים כאן.